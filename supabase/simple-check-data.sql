-- Simple check: View your curriculum data
SELECT 
  id,
  name,
  curriculum
FROM course_specializations
WHERE id = 3;

-- If curriculum shows up, the data IS there!
-- Now just refresh your browser and it should work.
